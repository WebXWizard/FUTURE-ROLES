# Commands

## Git

```bash
git clone <repo-url>
git status
git branch
git checkout <branch>
git pull
git log --oneline
```

## SQL

```sql
SELECT * FROM users;
SELECT * FROM users WHERE email = 'test@example.com';
SELECT COUNT(*) FROM orders;
SELECT status, COUNT(*) FROM orders GROUP BY status;
SELECT * FROM orders ORDER BY created_at DESC;
```

## API Checks

```txt
Check method, URL, headers, body, auth, status code, response body, schema, and database impact.
```

