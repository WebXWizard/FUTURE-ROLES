# Answer Framework

Use this structure:

```txt
Problem -> Data -> Baseline -> Model choice -> Metric -> Error analysis -> Improvement -> Deployment risk
```

Example:

```txt
For a fraud model, I would start with labeled transaction data, build a simple baseline, handle class imbalance, compare tree-based models, optimize recall and precision tradeoffs, analyze false positives, and monitor drift after deployment.
```

